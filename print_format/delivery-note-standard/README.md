# 发货单 - 标准打印格式

适用于 Delivery Note（发货单/交货单），用于销售发货、客户签收。

## 同步到 Print Format

```bash
bench --site junhai.local execute cos.scripts.sync_delivery_note_print_format.sync
```

## 导出 fixtures

```bash
bench --site junhai.local export-fixtures
```
